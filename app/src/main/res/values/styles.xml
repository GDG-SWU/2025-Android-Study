<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Welcome + to our store (큰 텍스트) -->
    <style name="WelcomeTitle" parent="@android:style/TextAppearance">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">48sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center</item>
        <item name="android:fontFamily">@font/pretendard_bold</item>
        <item name="android:lineSpacingExtra">4sp</item>
    </style>

    <style name="OnlineGrocText" parent="@android:style/TextAppearance">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:lineSpacingExtra">2sp</item>
        <item name="android:gravity">center</item>
        <item name="android:fontFamily">@font/pretendard_regular</item>
    </style>

    <!-- 설명 문구 -->
    <style name="DescriptionText" parent="@android:style/TextAppearance">
        <item name="android:textColor">#B2FFFFFF</item>
        <item name="android:textSize">16sp</item>
        <item name="android:lineSpacingExtra">2sp</item>
        <item name="android:gravity">center</item>
        <item name="android:fontFamily">@font/pretendard_regular</item>
    </style>

    <!-- 버튼 텍스트 -->
    <style name="ButtonText" parent="@android:style/TextAppearance">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">@font/pretendard_bold</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="BottomNavText" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textSize">12sp</item>
        <item name="android:lineSpacingExtra">8dp</item>
    </style>

</resources>